import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mysoft.MyKairos',
  appName: 'my-app-st',
  webDir: 'dist/my-app-st/browser'
};

export default config;
